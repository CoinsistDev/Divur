
export const addUserRole = async (user_id, role) => {
    // const { id: role_id } = await userRoleDal.getRoleId(role)
    // return userRoleDal.create({ user_id, role_id })
    return 'test'
}

export const getUserRole = async (user_id)=> {
    // const { role_id } = await userRoleDal.getUserRole(user_id)
    // const role = await userRoleDal.getRole(role_id)
    // return role.name
    return 'test'
}

